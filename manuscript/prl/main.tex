\documentclass[aps,prl,reprint,superscriptaddress,nofootinbib]{revtex4-2}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{hyperref}

\input{macros}
\input{generated/numbers}

\begin{document}

\title{Three local interactions of the ten-dimensional self-dual five-form\\
       that no stress-tensor flow generates}

\author{AUTHOR LIST --- HUMAN ACTION REQUIRED}
\affiliation{AFFILIATIONS --- HUMAN ACTION REQUIRED}

\date{\today}

\begin{abstract}
Non-linear theories of a chiral four-form gauge field in ten dimensions are
usually constructed by deforming a seed theory along a flow built from
stress-tensor invariants, as in lower dimensions where such flows generate
everything available. Whether that construction reaches the whole space of local
interactions in ten dimensions has not been decidable, because the space itself
was not known at any fixed order. We determine it exactly at quartic-squared
order: the space of local invariants of degree ten in the self-dual five-form is
\dimAten-dimensional, the subspace reachable by the stress-tensor flow is
\dimDten-dimensional, and the quotient is exactly \dimQten-dimensional. We give
explicit representatives of the three unreachable directions and show that
removing any one of them lowers the quotient, so three are necessary and three
suffice. Products of lower-degree invariants are all reachable, so the missing
directions are not built from anything simpler. The result is obtained in exact
arithmetic and recovered independently in graph, block-tensor and spinor
descriptions related by a certified equivariant map. Any deformation scheme
intended to cover ten-dimensional chiral four-form dynamics at this order must
therefore enlarge its generator set by at least three independent structures.
\end{abstract}

\maketitle

%=============================================================================
\paragraph*{Introduction.}
A chiral $p$-form gauge field carries a self-dual field strength, and in ten
dimensions that is the self-dual five-form $F$ of type IIB supergravity. Writing
a non-linear theory of such a field is delicate: the self-duality constraint is
not a Lagrangian equation of motion, and the standard route to interacting
theories is deformation --- start from the free theory and flow along a vector
field built from invariants of the stress tensor, the higher-dimensional analogue
of the $T\bar T$ construction~\cite{Hutomo:2025chiral}.

In four and six dimensions this is not merely a convenient method, it is
essentially the only one needed: the space of available local invariants is small
enough that the flow reaches it. Ten dimensions is structurally different. The
self-dual five-form has $\dimSelfDualModule$ components and the Lorentz group has
dimension $\dimLorentzAlgebra$, so a generic configuration has trivial stabiliser
and there are $\genericFunctionalDim$ functionally independent
invariants~\cite{Hutomo:2025chiral}. That the flow cannot be universal in this
setting is known. What has not been available is any statement of \emph{how} it
fails --- by how much, in which directions, and whether the shortfall is
generated by simpler structures.

The obstacle was that the space of invariants was not known at any fixed order.
Twelve explicit degree-ten structures appear in the
literature~\cite{Cederwall:2025invariants}, given as index expressions, not
claimed to be complete, and with their linear relations undetermined. Without the
ambient space one cannot ask what a construction misses.

We remove that obstacle at degree ten and answer the question exactly.

%=============================================================================
\paragraph*{Setup.}
Work in signature $(-,+,\dots,+)$ with $\eps_{01\ldots9}=+1$. For a five-form in
ten dimensions $\star^2 = (-1)^{p(d-p)}\sgn\det\eta = +1$, so real self-dual
forms exist; $\SD$ denotes the $\dimSelfDualModule$-dimensional space of them.

By a \emph{local invariant of degree $n$} we mean a Lorentz-invariant polynomial
of degree $n$ in the components of $F$, with no derivatives. These are the
scalars available to a local deformation at that order. Write

\begin{itemize}\itemsep2pt
\item $\Aten$ --- all degree-ten invariants;
\item $\Pten \subset \Aten$ --- the span of products of lower-degree invariants;
\item $\Dten \subset \Aten$ --- the span of degree-ten invariants generated by
      the stress-tensor flow from the seed theory;
\item $\Qten = \Aten/\Dten$ --- the obstruction quotient.
\end{itemize}

$\Dten$ is defined by the flow of Ref.~\cite{Hutomo:2025chiral}, generated from
the stress tensor of the free theory; its precise definition, and every
assumption below, is stated in the End Matter.

%=============================================================================
\paragraph*{Result.}
\begin{theorem}
Under the hypotheses stated in the End Matter,
\begin{equation}
  \dim\Aten = \dimAten,\qquad
  \dim\Dten = \dimDten,\qquad
  \dim\Qten = \dimQten ,
  \label{eq:main}
\end{equation}
and $\Pten \subset \Dten$. There exist three degree-ten invariants whose classes
span $\Qten$, and removing any one of them leaves a quotient of dimension
\dimQtenMinusOne, so three are necessary and three suffice.
\end{theorem}

Three consequences are worth separating.

\emph{The deficit is exactly three.} Not "at least one", which is what
non-universality alone gives, and not a bound --- an exact dimension, with
explicit representatives given in the End Matter.

\emph{The missing directions are irreducible.} Because $\Pten\subset\Dten$, every
product of lower-degree invariants is reachable. The three directions are
therefore not obtainable by composing lower-order results, and no iteration of a
lower-degree construction produces them.

\emph{The count is representation-independent.} $\Qten$ is computed in three
descriptions --- contraction graphs, block tensors built from
$M_\mu{}^\nu = F_{\mu abcd}F^{\nu abcd}$ and
$N_{abc}{}^{def} = F_{abclm}F^{deflm}$, and gamma-traceless symmetric
bispinors --- related by a certified equivariant isomorphism, and all three give
\dimQten.

%=============================================================================
\paragraph*{Method and verification.}
Everything is computed in exact arithmetic over finite fields rather than in
floating point. The reason is not fastidiousness: invariant values span an
enormous dynamic range at degree ten, so a floating-point rank requires choosing
a tolerance, and that choice is not innocuous. Two runs of the same
finite-difference calculation at different sample points reported ranks of $35$
and $81$; the discrepancy is entirely an artefact of normalising rows that had
decayed to rounding noise. Over $\mathbb{F}_p$, zero means zero.

Invariants are represented as contraction graphs, canonicalised so that two
labellings of the same contraction are identified, and evaluated at self-dual
five-forms sampled over $\mathbb{F}_p$. Dimensions are ranks of evaluation
matrices. Subspace claims are established by two-way containment, never by
matching dimensions: two different \dimBten-dimensional subspaces would otherwise
be indistinguishable, and at degree ten there really are three distinct ones.

Modular arithmetic bounds claims in one direction only, and we use it only in
that direction. A rank over $\mathbb{F}_p$ is an unconditional lower bound on the
rank in characteristic zero whenever the matrix is the reduction of an integer
matrix, which holds here because the coordinate basis is integral. Upper bounds
are never taken from a modular computation; where one is needed it is analytic.

The spinor description is connected to the tensor description by the map
$S_{ab} = \tfrac{1}{5!}F_{\mu_1\ldots\mu_5}(\Gamma^{\mu_1\ldots\mu_5})_{ab}$,
built with every convention pinned and verified: its kernel is exactly the
anti-self-dual $\dimAntiSelfdual$ and its image exactly the gamma-traceless
$\dimGammaTraceless$, both by span equality, with a left inverse that reproduces
the self-dual projector exactly. Equivariance is verified under products of
Clifford reflections, which generate the full rotation group.

One point about the frames is easy to get wrong and we state it explicitly. The
oscillator realisation used on the spinor side has real signature $(5,5)$, not
Euclidean: a null frame cannot be Euclidean, since Euclidean signature has no
isotropic vectors. Split and Lorentzian signatures both give $\star^2=+1$ on
five-forms, so real self-dual five-forms exist in each; but $(5,5)$ and $(1,9)$
are inequivalent real forms and the transition between them is not real. Over
$\mathbb{F}_p$, where only the discriminant survives, they are congruent and the
transition is exact --- which is where the component-level comparison is done.

\begin{figure}[t]
\includegraphics[width=\columnwidth]{figures/prl_spaces.pdf}
\caption{The degree-ten invariant space and its subspaces, with exact
dimensions. Products of lower-degree invariants lie inside the reachable sector,
so the \dimQten-dimensional quotient is not generated by anything of lower
order. Shading is redundant with the labels; the figure reads in grayscale.}
\label{fig:spaces}
\end{figure}

\begin{figure}[t]
\includegraphics[width=\columnwidth]{figures/prl_crossvalidation.pdf}
\caption{The same quotient in three descriptions. Arrows are verified maps;
numbers are ranks established by two-way span containment on a common sample
registry, in exact arithmetic at several primes.}
\label{fig:cross}
\end{figure}

%=============================================================================
\paragraph*{Physical consequence.}
The practical content is a statement about what a deformation scheme must
contain.

Fix any construction whose degree-ten output lies in $\Dten$ --- in particular
any flow generated by the stress-tensor invariants of the seed theory, iterated
to any order, and any composition of lower-degree results. Equation~\eqref{eq:main}
says such a construction misses a \dimQten-dimensional space of local
interactions, and the End Matter exhibits an explicit degree-ten invariant with
nonzero class in $\Qten$: no choice of flow parameters produces it, because its
image in the quotient is nonzero while every element of $\Dten$ has zero image.

The consequence for model building is concrete. A deformation scheme intended to
cover ten-dimensional chiral four-form dynamics at this order must enlarge its
generator set by at least three independent structures beyond the stress-tensor
sector, and three suffice at this order. That is a requirement on the
formulation, not a preference: it is forced by the dimension of the quotient.

We are careful about what this does \emph{not} say. It is an order-by-order
statement at degree ten, not an all-orders theorem. It constrains the interaction
space, not the dynamics of any particular theory, and it makes no statement about
supersymmetric completion, causality, or type~IIB coefficients.

%=============================================================================
\paragraph*{Discussion.}
The degree-ten interaction space of the ten-dimensional self-dual five-form is
now known exactly, together with the part of it that stress-tensor flows reach.
The gap is three-dimensional, explicit, and irreducible in the sense that it
contains no products of lower-degree invariants.

What changes conceptually is the status of the flow construction in ten
dimensions. In lower dimensions it is a complete parametrisation. Here it is a
proper subspace of known codimension, and the complement is small enough to write
down. The natural next questions are whether the codimension grows with order ---
degree twelve is not classified and we make no claim about it --- and whether
supersymmetry or causality selects among the three directions.

%=============================================================================
\begin{acknowledgments}
ACKNOWLEDGMENTS --- HUMAN ACTION REQUIRED.
FUNDING --- HUMAN ACTION REQUIRED.
\end{acknowledgments}

\bibliographystyle{apsrev4-2}
\bibliography{references}

\clearpage
\input{end_matter}

\end{document}
