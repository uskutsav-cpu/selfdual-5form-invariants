# Third-party code

No third-party implementation is currently vendored.

If the mentor's spinor implementation is added later, place it in a named
subdirectory only after recording its author, source revision, and license.
Do not copy formulas from the calculation note into this directory as if they
were original project code. See `docs/spinor_adapter.md`.
